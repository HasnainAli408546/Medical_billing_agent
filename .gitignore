# Environment
.env
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/

# DB
*.sqlite3
*.db

# Streamlit
.streamlit/credentials.toml

# Virtual Environment
venv/
backend/venv/
.pytest_cache/

# Large / generated files NOT needed in repo
archive/
archive.zip
not_used_dataset/

# Medical FAISS index is too large for GitHub (143 MB > 100 MB limit)
# The app falls back gracefully when this is missing (code search disabled)
# The smaller models (denial_model, billing rules FAISS) are still included
# backend/models/medical_index/
